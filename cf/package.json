{
  "name": "someday-cf",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:init": "wrangler d1 execute someday --remote --file=./schema.sql",
    "dev:test": "vite",
    "test": "playwright test"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.0.0",
    "@playwright/test": "^1.56.1",
    "leaflet": "^1.9.4",
    "typescript": "^5.5.0",
    "vite": "^5.4.21",
    "wrangler": "^4.0.0"
  },
  "dependencies": {
    "@cloudflare/containers": "^0.0.28"
  }
}
